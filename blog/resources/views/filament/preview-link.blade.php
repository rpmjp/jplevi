<div class="space-y-3">
    <input type="text" readonly value="{{ $url }}"
           onclick="this.select()"
           class="w-full rounded border border-gray-300 bg-gray-50 px-3 py-2 font-mono text-xs dark:border-gray-700 dark:bg-gray-900">
    <p class="text-xs text-gray-500">Click to select, then copy.</p>
</div>
